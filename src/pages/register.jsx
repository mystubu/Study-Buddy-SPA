import React from "react";
import registerRequest from "../services/register"

function Register() {
    return(
        <button onClick={registerRequest} >hihi</button>
    );
}

export default Register;