import React from 'react';
import ChatContainer from '../containers/ChatContainer';

const ChatView = () => {
    return (
        <div>
            <h2>Chat Room</h2>
            <ChatContainer />
        </div>
    );
};

export default ChatView;